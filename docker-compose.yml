version: '3.8'

services:
  api:
    build: .
    container_name: florida-pricing-api
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
      - FLASK_APP=api.py
    restart: unless-stopped
    # Add resource limits if needed
    # deploy:
    #   resources:
    #     limits:
    #       memory: 2G
    #     reservations:
    #       memory: 1G

